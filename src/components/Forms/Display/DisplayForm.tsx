import * as React from 'react';

import { Panel } from '@fluentui/react';

export type OFluiDisplayFormProps = {
    language?: string
}

export const OFluiDisplayForm = (_props: OFluiDisplayFormProps) => {
    return (
        <Panel hidden={true}>
        </Panel>
    );
};
